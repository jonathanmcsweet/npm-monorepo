'use strict'

module.exports = {
  install: require('./install'),
  publish: require('./publish'),
  link: require('./link'),
  run: require('./run'),
  test: require('./test'),
  clean: require('./clean')
}
